import numpy as np
from sklearn.linear_model import LinearRegression

# Dummy training data
X = np.array([[0.45], [0.50], [0.55], [0.60]])
y = np.array([25, 30, 35, 40])  # MPa

model = LinearRegression()
model.fit(X, y)

def predict_strength(w_c_ratio):
    return model.predict([[w_c_ratio]])[0]
